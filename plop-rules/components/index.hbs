import {{properCase name}} from './{{camelCase name}}.vue'

{{properCase name}}.install = function (Vue) {
  Vue.component({{properCase name}}.name, {{properCase name}})
}

export default {{properCase name}}
